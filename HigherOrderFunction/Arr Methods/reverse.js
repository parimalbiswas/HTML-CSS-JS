const fruits = ["Banana", "Orange", "Apple", "Mango"];
var xL = fruits.reverse();
console.log(xL);

// The reverse() method reverses the order of the elements in an array.

// The reverse() method overwrites the original array.